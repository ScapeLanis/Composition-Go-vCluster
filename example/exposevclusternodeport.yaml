apiVersion: v1
kind: Service
metadata:
  name: vcluster-nodeport
  namespace: composition-cluster
spec:
  selector:
    app: vcluster
    release: test-cluster
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
      nodePort: 30180
  type: NodePort
